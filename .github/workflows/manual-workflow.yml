name: 🔍 Manual Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 🛠️ Початок роботи
        run: echo "Ручний workflow працює успішно!"
